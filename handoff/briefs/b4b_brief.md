# B4b — Route ControlPlane storage through the storage traits + remote Arena-trial leasing (roadmap item 14, TD-13 second half, TD-12 last item)

Repo /home/user/hephaestus, branch lane/fable-closeout (checked out). Do NOT commit/push/switch branches; the orchestrator commits. No secrets; never delete or weaken a test; never run real codex/claude CLIs. 4 cores shared: one cargo command at a time.

## Today
- crates/hephaestus-ledger/src/storage.rs: `EventLedger` (append, replay_verified, head default) and `ArtifactBackend` (put, get, contains default) traits; SQLite `EventStore` + filesystem `ArtifactStore` implement them; a JSONL `FileEventLedger` and `MemoryArtifactBackend` (just landed) pass the same contract suite (`crates/hephaestus-ledger/src/contract.rs`). The doc comment at the top of storage.rs inventories every call site in the control plane.
- `ControlPlane` (crates/hephaestus-control/src/server.rs) holds `storage: Option<Storage { ledger: EventStore, artifacts: ArtifactStore }>` (grep `struct Storage`) and its verifiers/payload builders take `&ArtifactStore`/`&[StoredEvent]` (another agent just made every verifier work from an already-replayed history plus the artifact store — read the current code). The arena crate's `EvaluationStores` still opens `EventStore`/`ArtifactStore` concretely for write paths (`select`, `check`, `evaluate_and_record`).
- Remote workers: `WorkerRequest::{Lease, SubmitResult}` / `WorkerReply` (server.rs ~100-160), `handle_worker_request`, `lease_remote_job`, `record_remote_job_result` (~3089-3260), `remote_jobs: BTreeMap<String, RemoteJobRecord>` in `ControlState`, `hephaestus worker submit|status`, and the `hephaestus-remote-worker` binary (crates/hephaestus-control/src/bin/hephaestus-remote-worker.rs) which executes `execute_reference_worker_request(frame)`. Only a direct reference run can be leased. Arena paired trials are executed locally by `submit_arena_job`/the Arena job thread (grep `active_arena_job`, `ArenaJobRecord`, `run_paired_trials` or similar) using the pinned reference worker per role/task. docs/REMOTE_WORKERS.md, TECH_DEBT.md TD-12/TD-13, AUDIT.md item-14 paragraphs, ROADMAP.md item 14 "Not in this slice".

## Deliver
1. **Route ControlPlane storage through the traits (TD-13)**: make `Storage` generic or trait-object based (`Box<dyn EventLedger + Send>` / `Box<dyn ArtifactBackend + Send + Sync>`, or generics with a default type alias — pick the one that keeps `ControlPlane`'s public constructor signatures unchanged for existing callers) so that every canonical read/write in the control plane goes through `EventLedger`/`ArtifactBackend`. Extend the traits in the ledger crate only if a call site genuinely needs something the traits lack (then run the contract suite for all four backends). The arena crate's `EvaluationStores` should likewise accept any backend pair (generic over the traits, with the existing `open(paths)` constructor kept as the SQLite/CAS convenience). Prove it: add a `ControlPlane::open_with_backends(...)` (or equivalent) constructor and an in-process test that runs a real World/Genome registration + reference `run` + paired Arena evaluation + `arena select` + `replay` on the JSONL ledger + memory artifact backend, with identical projections/receipts to the SQLite/CAS path for the same inputs (compare the selection receipt bytes and the event hash chain). The default daemon (`hephaestusd`, `ControlPlane::open*`) keeps using SQLite/CAS; optionally expose `--ledger-backend sqlite|jsonl` on `hephaestusd` only if it is cheap and tested; otherwise leave it in-process and say so.
2. **Remote workers lease Arena trials (TD-12 last item)**: extend the remote job vocabulary so one paired Arena trial's role/task execution can be leased: when an Arena job is admitted with a new opt-in (e.g. `arena evaluate --remote` or a World Law / daemon flag — prefer a per-evaluation opt-in recorded in the admission record), each (role, task) reference-worker execution becomes a leasable unit (`WorkerReply::Leased` carries the same framed request the local worker would receive; the daemon signs and records the returned output exactly as the local trial path does, so the run.result_recorded event and the evaluation receipt are indistinguishable from local execution). Preserve: duplicate delivery idempotent, expired/revoked credentials fail closed before lease or result, lease timeout re-queues, the evaluation completes and scores only after every trial is recorded, and cancellation/kill still terminalizes the Arena job. The remote worker binary needs no change if the frame is the same; if it needs a job-kind field, keep backward compatibility. Tests: in-process test that a remote-leased paired evaluation produces the same receipt as the local path for the same World/Genomes; duplicate delivery of one trial result; credential expiry mid-evaluation failing closed with the job left pending, and the local path unaffected. Extend the existing spawned-daemon E2E for remote workers (another agent added `control_plane_e2e.rs` tests named like `remote_worker_*` — read them) with one leased Arena trial through the real binary if it stays well under 60 s.
3. **Docs**: docs/REMOTE_WORKERS.md, docs/LEDGERS.md (daemon now routes through the traits; which backend the daemon uses by default), TECH_DEBT.md (close TD-13; update TD-12 to what remains: mutation entries/CI job/coverage), AUDIT.md item-14 paragraphs and "Remaining product loop", ROADMAP.md item 14 "Not in this slice". Add new debt with the next free TD number for anything left.
4. checks/checks.json: keep every `find` anchor matching (server.rs has many); add at least two mutation entries (expired credential accepted for an Arena-trial lease; a remote trial result recorded without signature verification / for the wrong job). `python3 checks/target_gate.py --manifest checks/checks.json --root .` must pass.

## Gates (all, before reporting)
cargo fmt --all; cargo clippy --workspace --all-targets --all-features -- -D warnings; cargo test -p hephaestus-ledger; cargo test -p hephaestus-arena --all-features; cargo test -p hephaestus-control --all-features --lib (record wall time); cargo test -p hephaestus-control --all-features --test control_plane_e2e; cargo test --workspace --all-features --no-fail-fast; python3 checks/target_gate.py --manifest checks/checks.json --root .; python3 checks/docs_gate.py --root . README.md AUDIT.md ROADMAP.md docs/*.md; TS apps only if you touched protocol types they mirror.

Report: design, files, tests (names) and results, timings, docs, debt, `git status --short`.
