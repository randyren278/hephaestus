# A3 — Profile and speed up the control-crate test suite (roadmap perf goal)

Repo /home/user/hephaestus, branch lane/fable-closeout. Work in your worktree (verify base first as instructed in the launch message). Do NOT push; commit on your worktree branch. No secrets; never delete, skip, or weaken a test; never run real codex/claude CLIs. 4 cores shared: one cargo command at a time; use your worktree's default target dir.

## Facts so far
- `cargo test -p hephaestus-control --all-features --lib` = 124 tests, 368 s wall on the base commit (4 test threads), 386-488 s after the verification refactor depending on machine load. Per-test wall times (RUSTC_BOOTSTRAP=1 cargo test ... -- -Z unstable-options --report-time) under load, top of the list:
  321 s meta_evaluate_shows_a_descendant_strategy_reaching_equal_champions_at_lower_cost
  123 s gene_transfer_trials_record_contradiction_and_speciation
  117 s meta_evaluate_runs_two_lineages_and_records_a_replay_verified_receipt
  101 s injected_arena_scoring_failure_persists_failed_terminal_and_replays
   86 s canary_staged_rollout_promotes_through_champion_path_and_replays
   67 s evolve_start_completes_three_generations_with_one_promotion_and_replays
   60 s evolve_coding_bundled_world_completes_three_generations_from_the_example_fixture
   52 s canary_live_check_detects_a_genuine_latency_regression_and_rolls_back_the_champion
   46 s gene_bank_history_rejects_tampering_retyping_and_reordering
   46 s forge_assessment_records_verified_child_selection_and_replays
  Sum of all per-test times ≈ 1272 s; the wall clock is bounded by the single slowest test (the meta descendant test, ~18 paired Arena evaluations), so the critical path is "how long does one paired Arena evaluation take end to end in-process", times 18.
- One paired Arena evaluation: `ControlPlane::submit_arena_job` (crates/hephaestus-control/src/server.rs, grep it) admits the job, an Arena job thread runs parent then candidate trials serially, each trial = `SandboxManager::create` (crates/hephaestus-runtime/src/sandbox.rs: `git worktree add --detach` into a private root, later `git worktree remove`), a supervised launch of the pinned reference worker through the `hephaestus-process-guardian` binary (crates/hephaestus-runtime/src/supervisor.rs, guardian.rs), a signed `run.result_recorded` event, then the evaluator subprocess (`IsolatedEvaluator::score_guarded`, crates/hephaestus-arena/src/isolated_evaluator.rs), then terminal events; `refresh_projection` runs after every appended event and replays the whole ledger (with the `EvidenceCache` making evidence re-verification incremental). The daemon tick / test drain loops sleep 1-5 ms between polls (grep `sleep(Duration::from_millis` in server.rs and the drain helpers in server_tests.rs), so polling is not the problem unless a poll waits on a slow timer somewhere (check `SandboxManager::open(..., Duration::from_secs(30))`, guardian handshake timeouts, `wait`/`try_wait` loops, evaluator deadline handling).
- Another agent is concurrently (a) pinning the reference worker once per daemon instead of copying the binary per run, and (b) building the Forge mutation catalog. Do not touch `pin_reference_worker`, the evolve/forge code, or the arena verifiers; coordinate by keeping your change set to sandbox/supervisor/guardian/arena-job-thread/refresh plumbing and test helpers.

## Deliver
1. **Measure first**: add a temporary (not committed) timing harness or `eprintln!` timestamps and run ONE paired Arena evaluation in-process (e.g. the `forge_assessment_records_verified_child_selection_and_replays` fixture or a smaller existing test) to attribute wall time per phase: git worktree create/remove per trial, guardian+worker spawn to exit, result signing/append, evaluator spawn to exit, `refresh_projection` calls (count and total time), test drain-loop overhead. Also `strace -f -c` or `perf` if available (optional). Write the attribution table into your report.
2. **Fix the biggest costs without changing semantics or trust boundaries**, candidates (verify each with the measurement, keep only what helps):
   - Per-trial git worktree cost: reuse one detached worktree per (job, revision) with a clean checkout between trials (`git checkout -- . && git clean -fdx` is NOT acceptable if it weakens isolation between parent and candidate — a fresh worktree per trial is the isolation guarantee; instead look at making `git worktree add` cheaper: `--no-checkout` + sparse checkout when the World's tasks never read the tree, shared object store, `core.fsync`/`GIT_*` env for the test fixture repos, or creating worktrees for all trials of a job up front in parallel while trials run serially). Document any tradeoff.
   - Sequential trials: if the daemon runs parent and candidate trials strictly serially only for bookkeeping reasons, consider running the trials of one role concurrently up to a small bound while keeping event ordering deterministic (results appended in admitted order). Only if the ordering invariants (`advance_arena_trial` rejects out-of-order results) can be preserved by buffering; otherwise skip.
   - `refresh_projection` after every append inside a long pipeline: batch refreshes where a step appends several events in a row (refresh once at the end of the step), keeping every verifier call intact.
   - Guardian/worker startup: check for fixed sleeps, handshake timeouts, or `wait_for` polls with coarse intervals; check that the reference evaluator and worker are not rebuilt/copied/hashed more than needed per trial (hashing a multi-MB binary per trial adds up — cache by (path, mtime, len) → digest is NOT acceptable for the trust check; but hashing once per job instead of per trial is, if the identity check semantics are preserved).
   - Test-suite level: `cargo test` default is 4 threads here; the slowest test bounds the wall clock. Splitting the meta descendant test is not allowed (it is the proof), but if it currently does more evaluations than the proof needs (e.g. 3 lineages × 3 generations when 2 generations already show the cost difference), do NOT reduce it — instead make each evaluation faster.
3. **Measure after**: report `cargo test -p hephaestus-control --all-features --lib` and `--test control_plane_e2e` wall times before and after on the same machine state (note load), plus the per-phase table after.
4. Keep every existing test's assertions unchanged. Docs: update TECH_DEBT.md TD-16 paragraph if refresh behavior changed, docs/ARCHITECTURE.md or docs/CONTROL_PLANE.md if the Arena job pipeline changed materially. checks/checks.json anchors must still match (`python3 checks/target_gate.py --manifest checks/checks.json --root .`).

## Gates
cargo fmt --all -- --check; cargo clippy --workspace --all-targets --all-features -- -D warnings; cargo test -p hephaestus-runtime --all-features; cargo test -p hephaestus-arena --all-features; cargo test -p hephaestus-control --all-features --lib; cargo test -p hephaestus-control --all-features --test control_plane_e2e; python3 checks/target_gate.py ...; python3 checks/docs_gate.py --root . README.md AUDIT.md ROADMAP.md docs/*.md. Known load flakes to re-run alone before calling them failures: real_listener_services_status_and_shutdown_through_the_socket_writer, canary_* latency tests, drift_record_derives_from_verified_evidence_and_replays, provider_adapter_maps_exit_status_and_interrupt_to_completion_reasons.

Commit message: "Cut per-trial overhead in the Arena job pipeline" (adjust to what you actually did). Report: attribution tables before/after, changes, timings, gate results, commit hash, branch.
