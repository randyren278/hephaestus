# B1a — Forge mutation catalog + strategy knobs + per-mode evolve proofs (roadmap items 8, 10, 13; TD-14 knobs)

Repo /home/user/hephaestus, branch lane/fable-closeout (already checked out). Do NOT commit/push/switch branches; the orchestrator commits. No secrets; never delete or weaken a test; never run real codex/claude CLIs. 4 cores shared: one cargo command at a time.

## Today
- Forge's only mutation is `forge_prompt_mutation` in crates/hephaestus-control/src/server.rs (~7024): identity <-> ascii_uppercase; any other `ReferenceInstruction` (crates/hephaestus-runtime/src/reference_instruction.rs: 16 operations = identity/ascii_uppercase + 7 Gauntlet bad/fix pairs) is rejected "outside the Forge mutation scope". Replay: `verify_forge_history*` / `verify_forge_prompt` (server.rs ~6304-6460) recompute `expected_after` from `before` with the same flip rule.
- Failure clusters: crates/hephaestus-arena/src/clusters.rs — `cluster_trials`, `shape_signature`, `describe` -> `SuggestedMutation::ReferenceOperationFlip` only for `shape_case_mismatch`; receipt `algorithm = "failure-cluster-v1"`, recomputed on replay (`compute_analysis`, `verify_cluster_event`). `genome propose --analysis --cluster` (server.rs `resolve_forge_hypothesis`) binds a cluster to a proposal and rejects any cluster whose suggestion isn't the flip.
- Evolve: server.rs `advance_evolution` / `advance_evolution_generation` (~2372-2560) proposes exactly one child per generation with an operator-style hypothesis; `TRIALS_PER_GENERATION = 2` (crates/hephaestus-control/src/evolve.rs); `EvolutionStartedPayload`/`EvolutionGenerationPayload` in crates/hephaestus-control/src/protocol.rs (~1934); `verify_evolution_history` in evolve.rs cross-checks each generation's proposal/assessment/promotion.
- Meta strategies: `EvolverStrategyConfig` (protocol.rs ~2112: mutation_prioritization Fifo|CostWeighted, generation_count, experiment_allocation, candidate_count, gene_selection None|HighestTransferEffect, parent_strategy_id). `meta evaluate` (server.rs ~2150-2320) passes only generation_count/experiment_allocation into `EvolveStart`. docs/META_EVOLUTION.md and TECH_DEBT.md TD-14 say the other three knobs are recorded but not actionable.
- Gene Bank: crates/hephaestus-control/src/gene_bank.rs — a Gene records `operation_before`/`operation_after`; `gene transfer` applies it; `gene record` classifies transfer effects (positive/neutral/negative with effect bps).
- World mutation scope: `CompiledWorld::mutation_scope()` (crates/hephaestus-genome/src/world.rs) -> `MutationTarget::{Harness,Law,Evaluator}`; the compiler already rejects Law/Evaluator. Forge does not consult it today. Fixtures: every examples/gauntlet/<mode>/world.template.json and sealed-holdout/quickstart use `["harness"]`; examples/gauntlet/coding/world.template.json uses `[]`; the in-crate test helper `register_gauntlet_objects` (server_tests.rs ~10342) uses `[]`.
- Gauntlet fixtures: examples/gauntlet/<mode>/{world.template.json,invariants.json,parent.md (bad op),candidate.md (fix op),tasks/visible.json,tasks/sealed.json}; test `gauntlet_failure_modes_reject_the_bad_operation_and_pass_the_fix` (server_tests.rs ~10530) and `evolve_coding_bundled_world_completes_three_generations_from_the_example_fixture` (~10859, reads the bundled coding fixture) show how to drive real-worker evolve runs in-process; `examples/gauntlet/README.md` documents all of it.
- NOTE: another agent just refactored the arena verifiers to take an already-replayed history (`*_in` variants) and removed `EvidenceCache`; read the current code, not the line numbers above, which may have shifted.

## Deliver
1. **Mutation catalog** (new module, e.g. crates/hephaestus-runtime/src/mutation_catalog.rs re-exported, or crates/hephaestus-control/src/forge.rs — pick what keeps arena/control decoupled; the arena crate must be able to name target operations by string without depending on control): a deterministic, versioned table of one-step `agent.prompt` reference-operation edges. Every ordered pair (a -> b) of the 16 operations with a != b is a *representable* edge; the catalog groups them: `family` (identity/uppercase "casing" pair; one family per Gauntlet mode), `kind` (`Fix` bad->fix within a family, `Regress` fix->bad, `Flip` casing pair, `CrossFamily` otherwise). Authorization: a proposal for artifact `agent.prompt` requires `MutationTarget::Harness` in the World's `mutation_scope`; otherwise reject with a clear message ("World mutation scope does not authorize harness mutations"). Enforce this on NEW proposals (propose path). On REPLAY of `forge.proposed` events keep verifying the recorded `operation_before -> operation_after` edge: it must be a catalog edge (any representable edge), so every previously recordable receipt (the identity/uppercase flip) still verifies; also record the catalog version + edge kind in the proposal payload as new `#[serde(default, skip_serializing_if = Option::is_none)]` fields so old payload bytes stay canonical (payload canonicality is checked byte-for-byte on replay — read `decode_forge_proposal`/`validate_forge_event`). Update `examples/gauntlet/coding/world.template.json` and `register_gauntlet_objects` to `["harness"]` (they are fixtures for Forge-driven evolve; note it in docs). Any other test World that reaches Forge/evolve with `[]` gets `["harness"]` too — never weaken assertions, just make fixtures honest.
2. **Deterministic choice from failure clusters** (clusters.rs): bump to `failure-cluster-v2` with replay dispatch on the recorded `algorithm` (v1 keeps today's exact hypotheses/suggestions byte-for-byte so old receipts verify; v2 for new analyses). v2 `SuggestedMutation` gains `ReferenceOperation { operation_after: String }` (serde: keep `reference_operation_flip` variant spelled the same for v1). v2 needs the candidate's current operation: extend `cluster_trials`/`compute_analysis` with an `Option<&str>` current operation supplied by the control plane (it can parse the candidate's prompt from RegisteredObjects/CAS before calling the analysis; the arena API takes it as a plain parameter, recorded in the receipt as `candidate_operation: Option<String>` with skip_serializing_if so v1 receipts are unchanged; on replay the control plane re-derives it the same way and passes it in). Rule (deterministic, documented in the module doc): if the current operation is a Gauntlet "bad" op, every failure cluster suggests its family fix; if it is identity/ascii_uppercase, `shape_case_mismatch` suggests the casing flip (as today) and other shape clusters suggest nothing; a Gauntlet "fix" op suggests nothing (no regress). Never read sealed content.
3. **Strategy knobs steer search** (server.rs evolve + evolve.rs + protocol.rs + meta_evolve.rs):
   - `EvolveStart` gains an optional `strategy_id` (registered `meta_strategy`), recorded in `EvolutionStartedPayload` (skip_serializing_if none) and replay-checked against the registered strategy; CLI `hephaestus evolve start --strategy <id>` (crates/hephaestus-control/src/bin/hephaestus.rs) and `meta evaluate` passes each strategy's own id. Without a strategy the defaults are today's behavior (fifo, 1 candidate, no genes).
   - Per generation: run `forge analyze` on the diagnostic evaluation (the Champion is the analyzed candidate there — check `resolve_forge_hypothesis`'s parent/candidate role rule), order clusters by `mutation_prioritization` (Fifo = stable signature order, i.e. today's BTreeMap order; CostWeighted = descending `total_count`, ties by signature), take up to `candidate_count` distinct suggested target operations, and when `gene_selection == HighestTransferEffect` put first the Gene from the Gene Bank whose `operation_before` equals the Champion's operation with the highest recorded positive mean transfer effect (read gene_bank.rs projections). If no cluster suggests anything (e.g. no failures), fall back to the casing flip when the Champion is identity/ascii_uppercase (today's behavior), else record a generation with no candidates and finish the run with a new `EvolutionFinishReason::NoCandidateMutation` (add the variant; protocol + TS types in apps/hephaestus-tui and apps/hephaestus-web if they enumerate it — grep). Each candidate is proposed (analysis-bound hypothesis when derived from a cluster), evaluated against the Champion, invariant-checked, assessed; the highest-ranked `metrics_passed` child is promoted (same unmodified Champion policy). Trials per generation = 1 + candidates proposed; update the budget accounting (`TRIALS_PER_GENERATION` becomes the minimum) and `verify_evolution_history` to recompute trials from the recorded generation. `EvolutionGenerationPayload` gains `candidates: Vec<...>` (skip if empty) while the existing single-child fields keep describing the promoted or first candidate so old payloads stay canonical.
4. **Per-mode proofs**: one in-process test per Gauntlet mode (7) — e.g. a table-driven `evolve_promotes_the_fix_for_every_gauntlet_mode_from_the_bundled_fixtures` reading examples/gauntlet/<mode>/ files exactly like the evolve_coding test — that seeds the bad Genome as Champion, runs `evolve start` for 1-2 generations, and asserts generation 0 promoted a child whose prompt operation is the mode's fix through the unmodified Arena/selection/invariant/Champion policy, and that `replay` verifies. Plus focused unit tests: catalog edge classification and scope refusal; v1 receipt bytes unchanged (pin a v1 analysis fixture in a test); CostWeighted vs Fifo ordering; candidate_count=2 consuming 3 trials; gene_selection preferring a Gene; the same fixture with `mutation_scope: []` refusing to propose.
5. **Docs**: ROADMAP.md items 8, 10, 13 slices + AUDIT.md matching paragraphs + docs/EVOLUTION.md + docs/META_EVOLUTION.md ("What this proves today") + examples/gauntlet/README.md ("What this does not yet prove") + TECH_DEBT.md TD-14 (knobs now actionable; leave the async/polling and TUI parts open). State plainly that the reference runtime is a deterministic simulation and that the catalog is a closed table of 16 operations, not free-form prompt editing. Add new debt with the next free TD number for anything you leave.
6. checks/checks.json: update any mutation `find` strings you move (never remove entries); add at least three new mutation entries (scope check skipped, catalog edge check skipped on replay, CostWeighted ordering) with existing `mutation_test_commands`. `python3 checks/target_gate.py --manifest checks/checks.json --root .` must pass.

## Gates (run all before reporting)
cargo fmt --all; cargo clippy --workspace --all-targets --all-features -- -D warnings; cargo test -p hephaestus-arena --all-features; cargo test -p hephaestus-runtime --all-features; cargo test -p hephaestus-control --all-features --lib (record wall time); cargo test -p hephaestus-control --all-features --test control_plane_e2e; cargo test --workspace --all-features --no-fail-fast; python3 checks/target_gate.py --manifest checks/checks.json --root .; python3 checks/docs_gate.py --root . README.md AUDIT.md ROADMAP.md docs/*.md; if you touched TS types: (cd apps/hephaestus-tui && npm run typecheck && npm test) and same for apps/hephaestus-web (run `npm ci` first if node_modules is missing).

Report: design summary, files, tests added (names), per-mode proof results, timings, docs changed, debt added, `git status --short`.
