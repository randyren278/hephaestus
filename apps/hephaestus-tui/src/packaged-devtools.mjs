// The packaged operator UI runs with production assets only. Ink's optional
// React DevTools integration is intentionally inert in that distribution.
export default {
	initialize() {},
	connectToDevTools() {},
};
